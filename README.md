# My Simple Package

A simple Dart package that provides a greeting utility.

## Usage

```dart
import 'package:my_simple_package/my_simple_package.dart';

void main() {
  const greeter = Greeter('World');
  print(greeter.greet()); // Hello, World!
}
